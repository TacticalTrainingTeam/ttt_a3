class RscPendingInvitation {
    textureConnectionQualityPoor = QPATHTOF(empty.paa);
    textureConnectionQualityBad = QPATHTOF(empty.paa);
    textureDesyncLow = QPATHTOF(empty.paa);
    textureDesyncHigh = QPATHTOF(empty.paa);
    color[] = {1, 1, 1, 1};
    x = 0.939;
    y = 0.852;
    w = 0.054;
    h = 0.072;
    timeout = 10;
    blinkingPeriod = 2;
};
class RscPendingInvitationInGame {
    textureConnectionQualityPoor = QPATHTOF(empty.paa);
    textureConnectionQualityBad = QPATHTOF(empty.paa);
    textureDesyncLow = QPATHTOF(empty.paa);
    textureDesyncHigh = QPATHTOF(empty.paa);
    color[] = {1, 1, 1, 1};
    x = 0.939;
    y = 0.852;
    w = 0.054;
    h = 0.072;
    timeout = 10;
    blinkingPeriod = 2;
};
